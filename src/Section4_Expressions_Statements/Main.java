package Section4_Expressions_Statements;

public class Main {
    public static void main(String[] args) {
/*       int newScore = calculateScore("Tom", 500);
        System.out.println("New score is " + newScore);

        calculateScore(75);
        calculateScore();

    }

    public static int calculateScore(String playerName, int score){
        System.out.println("Player " + playerName + " scored " + score + " points.");
        return score * 1000;
    }

    public static int calculateScore( int score){
        System.out.println("Unnamed player scored " + score + " points" );
        return score * 1000;
    }

    public static int calculateScore(){
        System.out.println("No player name, no player score." );
        return 0;
        System.out.println(Section4_Expressions_Statements.areaCalculator.area(3));
        System.out.println(Section4_Expressions_Statements.areaCalculator.area(2, 4));

                    String stringYears = years + " y and ";
            if (years < 10 && years > 0) {
                stringYears = "0" + stringYears;
            }

            String stringDays = remainingDays + " d";
            if (remainingDays < 10 && remainingDays > 0) {
                stringDays = "0" + stringDays;
            }





        Section4_Expressions_Statements.MinutesToYearsDaysCalculator.printYearsAndDays(532800);
        Section4_Expressions_Statements.MinutesToYearsDaysCalculator.printYearsAndDays(5270400);
        Section4_Expressions_Statements.MinutesToYearsDaysCalculator.printYearsAndDays(0);



        Section4_Expressions_Statements.IntEqualityPrinter.printEqual(1,1,1);
        Section4_Expressions_Statements.IntEqualityPrinter.printEqual(1,1,2);
        Section4_Expressions_Statements.IntEqualityPrinter.printEqual(-1,-1,-1);
        Section4_Expressions_Statements.IntEqualityPrinter.printEqual(1,2,3);

*/

        System.out.println(PlayingCat.isCatPlaying(true, 10));
        System.out.println(PlayingCat.isCatPlaying(false, 36));
        System.out.println(PlayingCat.isCatPlaying(false, 35));
        System.out.println(PlayingCat.isCatPlaying(false, 24));
    }
}

